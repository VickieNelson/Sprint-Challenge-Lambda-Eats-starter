import React from "react";
import { NavLink } from "react-router-dom";

function Header(props) {
  return <></>;

  <header>
    <nav>
      <div>placeholder page title </div>
      <div>
        <NavLink>placeholder navlink</NavLink>
      </div>
    </nav>
  </header>;
}

export default Header;
